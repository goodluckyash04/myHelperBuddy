# ============================================================================
# myHelperBuddy - Python Package Requirements
# Django 4.2.x | Python 3.12+
# Finance-first rebuild — updated 2026-08-15
# ============================================================================

# Core Django Framework
Django==4.2.7
asgiref==3.7.2

# Environment Configuration
# Used in: mysite/settings.py via decouple.config()
python-decouple==3.8

# Authentication & Authorization (Google OAuth via allauth)
# NOTE: PyPI package name is "django-allauth", imports as "allauth"
django-allauth==0.57.0

# Security & Cryptography
# Used in: accounts/services/security_services.py (cryptography.fernet.Fernet)
# Used in: accounts/decorators.py
cryptography==41.0.7

# Date & Time Utilities
# python-dateutil: used by dateutil.relativedelta in views
# pytz: used internally by Django for timezone-aware datetimes (USE_TZ=True)
python-dateutil==2.8.2
pytz==2023.3

# Character Encoding (transitive deps of requests; pinned for reproducibility)
charset-normalizer==3.3.2
idna==3.6

# OAuth2 Dependencies (transitive deps of allauth)
oauthlib==3.2.2
requests-oauthlib==1.3.1
requests==2.31.0
urllib3==2.1.0

# JSON Web Tokens (transitive dep of django-allauth)
PyJWT==2.8.0
