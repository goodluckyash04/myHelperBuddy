# ============================================================================
# myHelperBuddy - Python Package Requirements
# Django 4.2.x | Python 3.12+
# Last reviewed: 2026-08-21
#
# Only DIRECT dependencies are listed here.
# Transitive dependencies are resolved automatically by pip.
# ============================================================================

# Core Django Framework
Django==4.2.7
asgiref==3.7.2

# Environment Configuration
# Used in: mysite/settings.py via decouple.config()
python-decouple==3.8

# Authentication & Authorization
# Used in: settings.py INSTALLED_APPS (allauth, allauth.account, etc.)
# NOTE: PyPI package name is "django-allauth", imports as "allauth"
django-allauth==0.57.0

# Google Services Integration
# Used in: accounts/services/google_services.py
#   - google.auth.exceptions
#   - google.oauth2.service_account, google.oauth2.credentials
#   - googleapiclient.discovery.build, googleapiclient.http.*
google-auth==2.23.4
google-api-python-client==2.108.0

# HTTP Requests
# Used in: accounts/services/google_services.py (requests.post to Google token endpoint)
requests==2.31.0

# Security & Cryptography
# Used in: accounts/management/commands/backup_db.py (cryptography.fernet.Fernet)
cryptography==41.0.7

# Image Processing
# Required by Django ImageField (accounts/models.py - UserProfile.profile_picture)
Pillow

# Date & Time Utilities
# Used in: accounts/views/views.py (dateutil.relativedelta.relativedelta)
python-dateutil==2.8.2
